[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @repo/tienda build"
  publish = "tienda/.next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"
